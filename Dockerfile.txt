# Use an official Node.js image as base (typo in image name)
FROM node:18-alpin

# Set the working directory inside the container
WORKDIR /app

# Copy package.json and package-lock.json first for caching
COPY package*.json ./app

# Install dependencies (wrong command)
RUN npm instal

# Copy the rest of the application files
COPY . .

# Expose the application port (invalid port)
EXPOSE abc

# Command to run the app (missing closing bracket)
CMD ["npm", "start"
